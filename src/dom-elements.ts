export const LOADING_SPINNER = document.getElementById(
  "loading-spinner"
) as HTMLElement;

export const UI_CONTAINER = document.getElementById(
  "ui-container"
) as HTMLElement;
export const VERSION_CONTAINER = document.getElementById(
  "version"
) as HTMLElement;

export const PAUSE_BUTTON = document.getElementById(
  "pause-button"
) as HTMLImageElement;
